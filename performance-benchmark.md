# hm-dianping 性能压测报告

本文记录本机环境下完成的三组 JMeter 压测：商家秒杀优惠券列表多级缓存、Redis Lua + Kafka + MySQL 秒杀全链路，以及 Redis 滑动窗口限流。所有数据均为本机实测结果，不代表生产环境容量。

## 1. 商家秒杀优惠券列表多级缓存压测

> 试验环境：本机 JMeter（`D:\tools\apache-jmeter-5.6.3`）｜ 1000 并发 / 1s 爬坡 / 持续 120s
> 接口：`GET /voucher/list/{shopId}`（按商铺查秒杀优惠券列表）
> 口径：性能数据取自**稳态窗口**；Caffeine 取扩端口后**复测稳态(1~113s)**，MySQL/Redis 取全窗口(起止连接抖动剔除误差可忽略)，结果均为本机实测、可复现。

### 1.1 实测核心指标（稳态）

| 方案 | QPS | 平均延迟 | P95 | P99 |
|---|---|---|---|---|
| MySQL（直查 DB） | ~7,833 | 127.1ms | 189ms | 259ms |
| Redis（L2） | ~10,868 | 91.5ms | 123ms | 151ms |
| Caffeine + Redis（L1+L2） | ~30,322 | 32.9ms | 60ms | 78ms |

提升幅度：
- 吞吐：Caffeine+Redis 相对 **MySQL ≈ 3.9×**，相对 **Redis ≈ 2.8×**。
- 平均延迟：127.1ms → 32.9ms，**降至 MySQL 的约 1/4**。
- P99：259ms → 78ms，降至约 **30%**。
- 稳态成功率：Caffeine 复测 **100%**。

> 压测注意点：高吞吐下曾出现 `java.net.BindException`/连接拒绝，实为 **JMeter 发压机 TCP 动态端口耗尽**（非应用瓶颈）；`netsh int ipv4 set dynamicport tcp start=1024 num=60000` 扩端口后复测无误，结论成立。

---

### 1.2 详细写法（适合技术汇报 / STAR 展开）

> **多级缓存优化（商家秒杀优惠券列表）**：基于 Caffeine + Redis 构建多级缓存架构，并对 **MySQL / Redis / Caffeine+Redis** 三种模式做同口径（1000 并发、持压 120s）压测；应用本地缓存后，接口吞吐由约 **7.8k req/s（MySQL）** 提升至约 **10.9k req/s（Redis）** 与约 **30.3k req/s（Caffeine+Redis）**，较 MySQL 提升约 **3.9 倍**、较 Redis 进一步提升约 **2.8 倍**；平均延迟由 **127ms** 降至 **92ms** 与 **33ms**（约为 MySQL 的 1/4），P99 由 259ms 收敛至 78ms。

### 1.3 简短写法（适合简历一句话 / 列表项）

> 设计并落地 **Caffeine + Redis 多级缓存**，完成 **MySQL / Redis / Caffeine+Redis** 三档同口径压测：QPS 约 **7.8k → 10.9k → 30.3k**，多级缓存较 MySQL 提升约 **3.9 倍**、较 Redis 再提升约 **2.8 倍**，平均延迟 127ms → 33ms（约 1/4）。

---

## 2. 高并发秒杀全链路压测

### 2.1 测试范围与口径

- 接口：`POST /voucher-order/seckill/{voucherId}`。
- 链路：Tomcat → Redis Lua 库存及购买资格校验 → Kafka 异步消息 → MySQL 扣减库存并创建订单。
- 工具：本机 JMeter 5.6.3，使用 1000 个独立登录令牌模拟 1000 个不同用户，每个用户请求一次。
- 数据：每档测试使用独立秒杀券，初始 Redis 与 MySQL 库存均为 1000。
- 加压方式：分别在 1、2、3、4、5 秒内启动 1000 个用户，观察吞吐、延迟、连接错误及最终一致性。

### 2.2 阶梯压测结果

| 1000 用户爬坡时间 | 吞吐量 | 平均延迟 | P95 | P99 | 成功请求 | 错误率 |
|---|---:|---:|---:|---:|---:|---:|
| 1 秒 | 648.9 req/s | 679.9ms | 1020.8ms | 1081.0ms | 806 | 19.4% |
| 2 秒 | 570.5 req/s | 55.1ms | 257.0ms | 362.0ms | 921 | 7.9% |
| 3 秒 | 364.8 req/s | 28.7ms | 189.0ms | 251.0ms | 994 | 0.6% |
| 4 秒 | **263.0 req/s** | **16.9ms** | **153.0ms** | **190.0ms** | **1000** | **0%** |
| 5 秒 | 209.9 req/s | 15.0ms | 122.0ms | 187.0ms | 1000 | 0% |

1～3 秒档位的失败请求均为 HTTP 建连阶段的 `HttpHostConnectException: Connection refused`，没有进入秒杀业务逻辑。4 秒档位是本机当前最高已验证的零错误档位，不能直接等同于生产环境最大 QPS。

### 2.3 Redis 与 MySQL 一致性验证

| 爬坡时间 | 秒杀券 ID | Redis 剩余库存 | Redis 下单用户数 | MySQL 剩余库存 | MySQL 订单数 |
|---|---:|---:|---:|---:|---:|
| 1 秒 | 12 | 194 | 806 | 194 | 806 |
| 2 秒 | 13 | 79 | 921 | 79 | 921 |
| 3 秒 | 15 | 6 | 994 | 6 | 994 |
| 4 秒 | 17 | 0 | 1000 | 0 | 1000 |
| 5 秒 | 14 | 0 | 1000 | 0 | 1000 |

各档位中，Redis 预扣库存、Redis 下单用户集合、MySQL 库存及订单数量完全对应；成功进入业务链路的请求均完成异步落库，未出现超卖，最终一致性校验通过。

### 2.4 结论

- 本机最高已验证零错误吞吐约为 **263 req/s**，平均响应时间 **16.9ms**，P99 约 **190ms**。
- 约 **365 req/s** 时开始出现少量建连失败，错误率为 **0.6%**；继续提高突发强度后错误率明显上升。
- Redis Lua、Kafka 消费和 MySQL 落库数量一致，说明异步下单闭环在本轮测试规模内工作正常。
- 本次没有独立记录 Kafka 峰值积压量及完全清空耗时，因此不在简历中填写未经测量的消息恢复时间。

主要报告：[4 秒零错误档位](performance/results/seckill-ramp4-retest-report/index.html)；测试计划：[seckill-burst.jmx](performance/jmeter/seckill-burst.jmx)。

---

## 3. Redis 滑动窗口限流压测

### 3.1 测试范围与口径

- 接口：`GET /shop/of/type?typeId=1`。
- 限流配置：方法级限流，5 秒窗口内最多放行 5 次请求。
- 加压方式：20 个并发用户在 1 秒内各发起一次请求。
- 判定方式：业务响应包含 `"success":true` 记为放行，否则记为被限流。

### 3.2 实测结果

| 阶段 | 请求数 | 期望放行 | 实际放行 | 实际拦截 | 阈值偏差 | 平均延迟 | P99 | 吞吐量 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 修复前 | 20 | 5 | 7 | 13 | 40% | 16ms | 53ms | 20.5 req/s |
| 修复后 | 20 | 5 | **5** | **15** | **0%** | 22.1ms | 55ms | 22.2 req/s |

所有接口响应均为 HTTP 200；JMeter 通过业务响应体区分放行与限流。修复后报告中的 75% 错误率对应预期的 15 次限流，不属于系统异常。

修复前，`limiter.lua` 每次请求都使用毫秒时间戳执行 `math.randomseed(now)`，再以“时间戳 + 随机数”作为 ZSet 成员。同一毫秒内的并发请求可能生成相同成员，`ZADD` 覆盖旧值后导致 `ZCARD` 少计请求数。

修复后，由 Java 为每次请求生成 UUID，并作为第 4 个参数传入 Lua；Lua 直接使用该值作为 ZSet 成员，避免并发请求相互覆盖，同时保留清理、计数、判断和写入的原子性。复测中实际放行数与配置值完全一致。

主要报告：[修复后限流压测报告](performance/results/rate-limiter-fixed-report/index.html)；测试计划：[rate-limiter.jmx](performance/jmeter/rate-limiter.jmx)。

---

## 4. 简历表述

### 4.1 多级缓存

> 基于 Caffeine + Redis 构建多级缓存，并完成 MySQL、Redis、Caffeine + Redis 三档同口径压测；接口吞吐由约 7.8k req/s 提升至 30.3k req/s，提升约 3.9 倍，平均响应延迟由 127ms 降至 33ms，P99 由 259ms 降至 78ms。

### 4.2 高并发秒杀

> 基于 Redis Lua 与 Kafka 构建异步秒杀下单链路，使用 JMeter 对 1000 个独立用户进行阶梯压测；本机最高验证零错误吞吐约 263 req/s，平均响应时间 16.9ms、P99 约 190ms，压测后 Redis 与 MySQL 库存及订单数量完全一致，未出现超卖。
